{
  "name": "@glyphquire/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:start": "sh scripts/production-start-smoke.sh",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@glyphquire/api-contract": "workspace:*",
    "@glyphquire/database": "workspace:*",
    "@glyphquire/queue": "workspace:*",
    "@glyphquire/shared": "workspace:*",
    "@glyphquire/storage": "workspace:*",
    "drizzle-orm": "^0.44.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^2.1.0"
  }
}
